// import React, {useDispatch} from 'react';

// const dispatch = useDispatch();

// export const addTransaction = (transaction) => dispatch({type: 'ADD_TRANSACTION', payload: transaction});
// export const deleteTransaction = (id) => dispatch({type: 'DELETE_TRANSACTION', payload: id})
